{
  "name": "@leadsy/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "smoke:extension": "playwright test tests/e2e/extension-smoke.spec.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "@types/node": "^24.0.0",
    "@playwright/test": "^1.57.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^26.0.0",
    "vitest": "^4.1.8"
  }
}
